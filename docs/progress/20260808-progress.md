# Progress Log — 2026-08-08

Direct continuation of 2026-08-05's Session 3 (859's rebate-table extraction fixed
properly, scrape plan for its age x term grid prepared but not run) - this session
completed policy 859's premium lookup end to end.

## Built 859's full premium lookup from the completed scrape

User completed all 52 scraped points from the 2026-08-05 plan (ages 18/25/30/35/40/
45/50/55/60/65, every valid term at 5-year steps, capped by 859's own
`age + term <= age_at_maturity_max` of 70). Ingested into
`docs/progress/ground-truth/859_scraped_premiums.csv`, same schema as 876's CSV
(distribution_channel=offline, gender=male, non-smoker, regular/annual, all at
SA=500,000 - 859's own `sum_assured_min`/Nil-rebate band).

**Generalized `query/build_premium_lookup.py`** from a single 876-hardcoded script
(module-level `POLICY_ID`/`LAYER1_PATH`/`REFERENCE_SUM_ASSURED`/etc. constants) into
a `POLICIES`-list-driven build - every function now takes a `cfg` dict instead of
reading module globals, and `main()` loops over `POLICIES`, writing every entry into
one `premium_lookup.json`. Adding 859 was a new list entry (own reference SA, own
ground-truth CSV, no `age_bands`/`rebate_sa_type_key`/`rebate_premium_type_key` at
all since its rebate table has no age dimension), not a rewrite - confirms the
2026-08-05 generalization work (typed rebate tables, `compute_sa_adjustment()`
dispatch) was pointed in the right direction. `build_sa_scaling()` also generalized
to dispatch on the rebate table's own `type` directly (previously hardcoded to raise
on anything but `percent_of_tabular_premium`) - a `per_mille_of_sum_assured_rupees`
policy now builds with an empty `qa_check` per band (859 has no cross-SA ground
truth yet, all 52 scraped points are at the reference SA itself) rather than being
unbuildable.

**Generalized `query/validate_premium_lookup.py`** to take an optional `policy_id`
CLI arg (defaults to 876, so the existing bare invocation still works unchanged).

**Generalized `query/premium_interpolation.py`'s dispatch** - removed the
hardcoded `_PREMIUM_LOOKUP_SCOPE` constant entirely. Each `premium_lookup.json`
entry already carries its own `premium_payment_option`/`sum_assured_type`, so the
dispatch now just checks `policy_id in _premium_lookup_table` and compares the
profile against that entry's own stored scope - no separate scope constant to keep
in sync as more policies get added.

## Verified no regressions, then validated 859 on its own merits

- Rebuilt `premium_lookup.json` for both policies in one run: 876 unchanged (41
  age/term points, all 4 QA cross-checks against real scraped data still pass
  exactly), 859 new (52 age/term points, `per_mille_of_sum_assured_rupees` bands
  printed with no QA mismatches - none expected yet, no cross-SA ground truth).
- `premium_interpolation.py`'s full hand-check suite (877, 875, 876 level/increasing,
  end-to-end eligibility->budget scenarios) still passes unchanged, including 876's
  precomputed-lookup dispatch case (`premium_amount=4700.0`).
- **859 leave-one-out validation: median error 1.66%, mean 2.64%, max 11.12%**
  (age=45/term=5) - notably no thin-anchor-driven outlier like 876's 64.78% one,
  because 859's valid (age, term) domain is a clean triangle (not a scattered
  shape like 876's), so the scrape could cover it exhaustively at 5-year steps in
  one pass rather than needing 876's scatter-then-gap-fill approach.
- Confirmed the precomputed dispatch actually fires for 859 regular/level profiles
  (not just that the build succeeded): age=30/term=20/SA=5L (the reference point)
  returns exactly 2095, matching the brochure exactly, same brochure-matching
  sanity check pattern as 876's 4700 case.

## Independent spot check against a real LIC quote well outside the scraped grid

User supplied a real LIC calculator screenshot mid-session: age=31, term=20,
PPT=20, SA=Rs 25,00,000 (5x the reference SA scraped), Half Yearly premium
including tax = Rs 5,246 (tax=Rs 0). This SA was never scraped at all (all 52
points are at SA=5L) and age=31 isn't one of the scraped ages either, so this is
a genuine independent check on both axes at once, not just the SA formula alone.

Our lookup predicts Rs 10,420 (annual) for this exact profile. Converted the real
half-yearly quote to an annual-equivalent using 859's own `mode_loadings`
("Half-yearly mode: 2% of Tabular annual premium", loading added on top and split
across 2 installments): `implied_annual = hy_quote * 2 / 1.02` = Rs 10,286.
**1.3% spread** - a solid independent confirmation, not derived from or feeding
back into the scraped grid itself.

## Docs updated
`docs/schema.md` (premium-lookup artifact section, ground-truth CSV section),
`docs/query_architecture.md` step 5, `README.md` status, `CLAUDE.md` build command
(now describes the generalized, per-policy `POLICIES`-driven build and
`validate_premium_lookup.py`'s optional policy_id arg).

## Session 2: closed the cross-SA QA gap flagged at the end of Session 1

User scraped 4 more real points at higher sum assureds, chosen to land on
existing scraped (age, term) anchors (age 30/term 20 and age 45/term 15) so
the cross-check isolates the SA-axis formula cleanly rather than blending in
age x term interpolation error - same principle as 876's QA-check design:
- age 30/term 20: SA 10L -> Rs 4,090; SA 25L (859's real max) -> Rs 9,850
- age 45/term 15: SA 15L -> Rs 12,990; SA 25L -> Rs 21,525

Hand-checked against `compute_sa_adjustment()`'s formula before touching any
code - all 4 matched exactly (e.g. age30/term20 baseline 2095 x 5 - 625 =
9850). Appended to `859_scraped_premiums.csv` (56 rows total now) - one bug
caught immediately: the notes field for these new rows contained a literal
comma inside an unquoted CSV field, silently producing 17-field rows instead
of 16; caught by re-parsing the file right after writing it, not assumed.

**Replaced `build_sa_scaling()`'s `per_mille_of_sum_assured_rupees` branch**
(previously a stub emitting `qa_check: []` unconditionally, since 859 had no
cross-SA data when it was written) with a real check: `_per_mille_checks_by_sa()`
groups ground-truth rows by (age, term), finds pairs where a reference-SA row
and a higher-SA row exist for the exact same query, and computes what the
formula predicts vs. what was actually scraped - kept as raw age/term-tagged
Rupee comparisons (`formula_premium`/`actual_premium`) rather than averaged
ratios like the percent-type check, since this formula subtracts an absolute
amount rather than applying a pure ratio, so it isn't age/term-invariant the
same way. Rebuilt: **all 4 new QA checks pass exactly**, including SA=25L
checked independently at two different ages (30 and 45) landing on the same
formula prediction both times - a real, if informal, confirmation that this
rebate table's per-mille rate genuinely doesn't vary by age, matching the
brochure's own table (no age column at all).

Re-ran the full regression suite (876/859 leave-one-out validation, the
`premium_interpolation.py` hand-check suite) - unchanged, since the 4 new
rows are all cross-SA and correctly excluded from `age_term_grid` (only
`sum_assured == reference_sum_assured` rows feed that axis).

## Open / not done
- Term 15's thin coverage on 876 (earlier session's flag) - unrelated to this
  session's work, still not scraped further.
- The other 5 term-assurance policies (875, 877, 878, 954, 955) still hold the
  old prose/placeholder `rebate_structures` and haven't been re-extracted against
  the current type-tagged prompt.
- The age x term shape-transfer question (876 vs. 877, raised 2026-08-05) is
  still untested - 859's build doesn't bear on it either way, since 859's own
  curve was scraped directly rather than derived from another policy's shape.

## Session 3: first real shape-transfer data point, caught and corrected a self-made documentation error, started 875

**Shape-transfer question actually tested for the first time**, using 876 and
859's now-both-real scraped datasets. Normalized each policy's premiums to
its own age-18 value at matching terms: the age curves diverge 12-33%, with
the gap widening at older ages (not a constant offset); the term curve
matches within ~1% at age 18 but drifts to ~7% by age 45. Conclusion: shape
does NOT transfer cleanly between these two specific policies - flagged as
n=2 and a poor test pair (859 is a standardized cross-insurer product, 876 is
LIC's own online product), not a general verdict yet. Full numbers logged in
`docs/query_architecture.md`'s open questions.

User asked why 877 was suggested as the next test candidate. Checked and it
was a bad suggestion - 877 is `structural_variant: "credit_life"` (decreasing
SA tied to a loan schedule), a different pricing mechanism entirely, not a
fair mortality-curve comparison. Checked all 7 term policies'
`structural_variant` properly this time; picked **875 (Yuva Term)** instead -
`standalone` like 876/859, and shares 876's exact 5,000,000 reference SA.

**Caught and corrected a real error from the 2026-08-05 session**, while
checking 875's fields with the correct schema names. The 2026-08-05 log (and
`docs/schema.md`) claimed re-extracting 859 fixed two "bonus" bugs beyond the
rebate table: `entry_age_min`/`entry_age_max` null, and `plan_name` null.
Both were false - `entry_age_min`/`entry_age_max` isn't a real field in this
schema at all (the real name is `age_at_entry_min`/`age_at_entry_max`, which
was already correctly `18`/`65` in 859's pre-existing file); `plan_name` was
also already correct before any re-extraction. Verified against git history
directly, not just re-reading the current file. Also found the *separate*,
genuinely real "`plan_name` null for all 7 policies" bug (found legitimately
on 2026-08-01) was itself already resolved by an unrelated 2026-07-27
re-extraction (`8494330`) - predating even the note that first flagged it as
open, meaning that open question had been stale for over a week without
anyone reconciling it against actual file contents. Corrected `docs/schema.md`,
`docs/query_architecture.md`, and the 2026-08-05 progress log itself (as a
correction note, not a silent rewrite) - all three committed separately.
**Lesson: verify a field name against the actual schema (`docs/schema.md`)
before treating a `None`/missing value as a finding, and re-check an "open"
data-gap claim against current file contents before citing it as still true.**

**Re-extracted 875's Layer 1 + Layer 2** through the real pipeline (not a
hand edit), same process as 859/876. Confirmed against the raw brochure PDF
first: 875's `high_sum_assured_rebate_table` was still the old pure-placeholder
prose ("Provided in brochure under Section 8...", zero real numbers) - one of
the original 2026-08-01 audit's "875/878/954, pure placeholder" bucket. The
real brochure table is genuinely structured (same `percent_of_tabular_premium`
shape as 876: 4 SA bands x 2 age bands x level/increasing x regular/single),
with different rates than 876's own table (e.g. up_to_30/level/regular: 876 is
0/18/30/37, 875 is 0/18/33/40 - confirms two independently-priced tables, not
a copy error). Re-extraction matched the PDF exactly and held the tightened
trap-17 structure (`"table"`, numeric `sa_bands`) on the first attempt this
time, unlike 876's first pass. `sample_illustrative_premiums` identical
(24/24 match). Layer 2 identical `group_a_concern_tags`/`group_c` bounds.

**Prepared a 33-point scrape list for 875's age x term grid** - its full
valid (age, term) domain (entry_age 18-45, policy_term 15-40, capped by
`age + term <= age_at_maturity_max` of 75; its `age_at_maturity_min` of 33
turns out never binding, since the minimum term of 15 already puts every
age at or above that floor). Same 5-year-step convention as 859's grid,
deliberately using the same age anchors (18/25/30/35/40/45) so 875's curve
can be compared directly against both 876 and 859 once scraped. Not yet
executed.

## Open / not done (Session 3)
- 875's 33-point scrape not yet run - `build_premium_lookup.py` needs a new
  `POLICIES` entry for 875 once it is (same shape as 876's entry: age_bands,
  rebate_sa_type_key, rebate_premium_type_key all apply, since 875 is the
  same rebate type as 876).
- The other 4 term-assurance policies (877, 878, 954, 955) still hold the old
  prose/placeholder `rebate_structures` (877/955 have real numbers flattened
  to prose; 878/954 are pure placeholders) and haven't been re-extracted.
- The shape-transfer finding above is still n=2 and needs 875's real data (or
  a third/fourth policy) before it's more than a suggestive first data point.

## Session 4: closed the rebate_structures gap on the remaining 4 term-assurance policies

While the 875 scrape ran in the background, user asked to verify all 7
policies' extraction state before continuing - confirmed only 859/875/876
were actually clean; 877/878/954/955 still held the old
prose/placeholder `rebate_structures`, exactly matching the original
2026-08-01 audit's classification (878/954 pure placeholders, zero numbers;
877/955 real numbers flattened to prose). Checked all 4 against their real
brochure PDFs before re-extracting anything, confirming real numeric data
exists for all 4 and noting 877's SA bands start at a different floor
(20L, not 50L like 876/875/878) and 954/955 use a 3-age-tier x 3-SA-band
shape, not 876's 2-tier x 4-band shape - three genuinely different table
shapes now confirmed across the 7-policy corpus.

**878, 954, 955 came back clean on the first (878 needed one field fix) or
second attempt**, verified against their PDFs field-by-field, not just the
rebate table:
- 878: rebate table matched exactly on attempt 1, but `premium_payment_options`
  wrongly included `"regular"` - the brochure explicitly states only
  "Limited Premium or Single Premium payment options" are offered. Fixed on
  attempt 2 (re-ran the same prompt, no prompt change needed - not
  reproducible the way 877's issue below was).
- 954: clean end to end on attempt 1 - rebate table, eligibility bounds,
  sample premiums (content-identical to prior extraction) all verified.
- 955: clean end to end on attempt 1, same verification depth.
- Layer 2 re-derived for all 3; `group_a_concern_tags` shifted slightly on
  954 (gained `debt_linked_cover`) and 877 (see below) - not treated as a
  bug, Group A is explicitly an interpretive judgment layer, not a
  verifiable fact, so run-to-run variance there is expected and different in
  kind from the deterministic-field bugs found elsewhere this session.

**877 was the hardest policy this project has extracted so far - 3 attempts
plus one narrow hand-correction.** Rebate table was correct on every single
attempt (including correctly capturing the real 20L SA-band floor). But:
- Attempt 1: `sum_assured_max` came back 500000000, a clean 10x of the
  brochure's clearly-printed 50000000 ("Maximum Basic Sum Assured: Rs
  5,00,00,000") - checked the policy_doc too in case of a genuine
  brochure/policy_doc conflict per the merge rule, found nothing there
  either (pdftotext search for "crore" and "maximum sum assured" in the
  policy_doc returned zero hits), so this looks like a straightforward
  extraction error, not a document ambiguity. Also had a schema-violating
  sample-premium row (`premium_payment_option: "single"` with
  `payment_mode: "annual"`, directly contradicting trap 12's own explicit
  rule).
- Attempt 2 (re-ran the identical prompt/inputs): fixed both attempt-1
  issues, but this time `premium_payment_options` wrongly included
  `"regular"` - reproducing a **pre-existing** error that had been sitting
  unnoticed in the old file all along (confirmed via the same brochure text
  checked for 878: "Premiums can be paid either under Limited Premium or
  Single Premium payment options").
- Attempt 3: fixed the `premium_payment_options` issue too (finally matching
  what the PDF actually says), and `sum_assured_max` stayed correct this
  time. But it introduced yet another, different error: one sample-premium
  cell (age 40, the PPT-15 column, `premium_amount: 11650`) got labeled
  `limited_ppt_10` instead of `limited_ppt_15` - a *third* distinct wrong
  label for the exact same cell across 3 attempts (attempt 1 had called it
  `single`). All 3 attempts got the premium amount itself right, only the
  column label wrong, differently each time - checked directly against the
  brochure's own sample-premium table (age 40 row: Single=1,03,450,
  PPT5=25,100, PPT10=14,900, PPT15=11,650) to confirm which label was
  actually correct.
- Asked the user how to handle this last cell rather than deciding
  unilaterally - flagged as the first case this project has hit where 3
  independent pipeline runs couldn't produce one specific field correctly,
  while a pre-existing (unrelated-reason) extraction happened to have it
  right. User chose to fix the one field and flag it. Corrected
  `sample_illustrative_premiums[11].premium_payment_option` by hand,
  added a `field_specific_notes` entry with the full reasoning (all 3
  attempts' different wrong values, the brochure table's real numbers, why
  this is a narrow exception) - the first and only hand-correction in this
  project's re-extraction work so far, deliberately scoped to one cell,
  not a shortcut around re-running the pipeline.
- Canonical filenames adopted (`layer1_877.json`/`layer2_877.json`), old
  non-canonical `*_flashlite_pdf_v3_run1.json`/`*_run1.json` files removed.

**Lesson, logged in `docs/schema.md`:** verifying only the target field
isn't enough once a re-extraction is a full model call, not a patch - other
fields can and did regress even when the field being fixed came back
correct every time. Every re-extraction this project has done since
2026-08-05 has been diffed field-by-field against the prior file and
cross-checked against the source PDF before adopting; 877's issues would
have shipped silently (a 10x-wrong `sum_assured_max`, most notably) under a
narrower "just check the rebate table" verification process.

**Result: all 7 term-assurance policies now have real, typed, pipeline-
verified `rebate_structures`** - the gap first found 2026-08-01 is fully
closed. Full regression suite re-run after all 4: `load_extracted.py` loads
7/7 policies at both layers, `premium_interpolation.py`'s hand-check suite
unchanged (876's precomputed-lookup case still `premium_amount=4700.0`),
`check_concern_tags_sync.py` clean.

## Open / not done (Session 4)
- 875's 33-point scrape still in progress (started before this session,
  continuing in parallel) - not yet built into `premium_lookup.json`.
- 877/878/954/955 now have real rebate/SA-axis data ready, but none have a
  built premium lookup yet - each still needs its own age x term scrape
  before `build_premium_lookup.py` can add a `POLICIES` entry for it (same
  pattern as 875/859).
- The shape-transfer question is still n=2 (876 vs 859) - unaffected by this
  session's work, since none of 877/878/954/955 got an age x term scrape.

## Session 5: min/max sum assured + entry age check across all 7 policies; 875 scrape partially landed

User asked whether `sum_assured_min`/`sum_assured_max` and entry-age bounds
exist for all 7 policies, referencing the earlier finding that 954/955 show
`max_sum_assured: null`. Confirmed directly against the current extracted
files: all 7 have real, populated `sum_assured_min` and `age_at_entry_min`/
`age_at_entry_max`. The two `null` `sum_assured_max` values (954, 955) are
not a gap - both brochures literally state "No Limit* As per underwriting
decision", and `docs/schema.md`'s own Layer 1 schema already documents this
exact case as an expected nullable value, not something to flag. Entry-age
ranges split cleanly by product type: the 4 term-life-only policies
(875/876/877/878) cap at 45; 859/954/955 go to 65.

**875's scrape (started in the 2026-08-08 Session 3/4 work, run in a
separate/parallel session) hit that session's token limit at 29/33 rows
reported.** User pasted the partial results back row-by-row. Rows 2-29
matched the original 33-point list's count column exactly (DOB x term pairs
for ages 18/25/30/35/40, terms 15-40 per the domain caps) and were saved to
`docs/progress/ground-truth/875_scraped_premiums.csv` (28 rows - one entry,
row 6's DOB-2008/term-20 point at 4450, through row 29's
DOB-1986/term-35 point at 18350). **Row 1 (DOB 15/01/2008, term 15, age 18)
was not present in the pasted transcript** - unclear whether it was
reported earlier and lost in the paste, or never actually completed; flagged
back to the user rather than assumed either way. **Rows 30-33 (DOB
15/01/1981, age 45, terms 15/20/25/30) are confirmed not yet done.**

## Open / not done (Session 5)
- 875's ground-truth CSV has 28/33 confirmed rows. Still needed: row 1
  (age 18, term 15 - status unclear, needs re-confirming) and rows 30-33
  (age 45, terms 15/20/25/30).
- Once complete, `build_premium_lookup.py` needs a new `POLICIES` entry for
  875 (same `percent_of_tabular_premium` shape/config as 876's entry, per
  Session 3's plan), then the usual rebuild + leave-one-out validation +
  hand-check regression pass before adopting, same process as 859.

## Session 6: 875's scrape completed, lookup built, and the shape-transfer question answered for this pair

User delivered the final 5 rows (row 1: age 18/term 15/₹4,350; rows 30-33:
age 45/terms 15-30/₹14,900-₹23,500). Appended to
`docs/progress/ground-truth/875_scraped_premiums.csv`, now 33/33, re-verified
16-field/33-row parse cleanly.

Added 875's `POLICIES` entry to `build_premium_lookup.py` (own reference SA
5,000,000 - same figure as 876's, since both share `sum_assured_min`; same
`age_bands`/`rebate_sa_type_key`/`rebate_premium_type_key` config as 876's
entry, confirmed against 875's own extracted rebate table rather than
assumed identical). Rebuilt all 3 policies in one run - 876/859 unchanged,
875 got 34 age/term points (33 scraped + 1 brochure point at the same
reference SA, no conflict). Leave-one-out validation: median error 1.23%,
mean 2.64%, worst case 11.86% at term=15 (the same edge-sensitivity 876's
own term-15 slice showed - not a new problem, an inherent property of having
nothing below the domain's minimum term to bracket from). Full regression
suite re-run: `premium_interpolation.py`'s hand-check for 875
(age=30/term=20/regular) now correctly dispatches through the new
precomputed path (`method` field shows
`bilinear_..._x_percent_of_tabular_premium_x1.0`) and still returns 5950.0,
matching both the brochure and the pre-existing linear-interpolation value -
no regression. `check_concern_tags_sync.py` clean.

**Answered the actual question 875 was chosen to test: does 876's mortality-
curve shape transfer to a structurally similar sibling policy?** Compared
875's real scraped curve against 876's directly (both share the exact same
5,000,000 reference SA, so no normalization assumption needed beyond ratio
comparison). Result: **yes, closely** - 0.6-2.4% deviation across every
age x term point checked (age curve normalized to age 18, term curve
normalized to term 20), including at the oldest ages where 876-vs-859
diverged most (30-33% there, vs. 2.1-2.4% for 875-vs-876 at the same
points). This is real, if still thin (n=1 confirming pair vs. n=1 diverging
pair), evidence for the hypothesis raised 2026-08-08 earlier today: 875 and
876 are both LIC's own modern online-style term products sharing a
reference SA and rebate-table shape, while 859 is a standardized
IRDAI-templated product priced across all insurers - mortality-curve shape
plausibly transfers *within* a product family, not across a structurally
different one. Logged the full comparison in `docs/query_architecture.md`'s
open questions, updated `docs/schema.md`'s premium-lookup artifact section
and `README.md`'s status to match.

## Open / not done (Session 6)
- Shape-transfer finding is n=1 confirming + n=1 diverging - not enough to
  build a zero-scrape shortcut on, but enough to motivate "check which
  product family a new policy resembles first" as a real next step, e.g. a
  cheap partial-grid spot-check (4-6 points) before committing to either a
  full scrape or full trust in an existing curve.
- 877/878/954/955 still have no built lookup - real rebate/SA-axis data
  ready, but each needs its own age x term scrape (or a spot-check against
  875/876's shape, per the finding above, before deciding how much to
  scrape).
- **User hypothesis, not yet tested: the mortality-curve shape match found
  above might correlate with *when a policy was launched/revised*, not
  purely `structural_variant` similarity** - i.e. 875/876 could track
  closely because they're contemporaneous products off the same underlying
  mortality table vintage, not (or not only) because they're both
  `standalone`. No `launch_date`/`revision_date` field exists in Layer 1
  today. Quick circumstantial check: UINs are suggestive - 875/876 are
  `512N355V02`/`512N356V02` (adjacent numbers, same version), while 859 (the
  outlier) is `512N341V01` (a notably different number and version) - but
  this is confounded, since 859 is also a structurally different
  (standardized, cross-insurer) product, so UIN proximity and structural
  similarity both point the same direction for this one pair and can't be
  disentangled from just these 3 policies. Worth checking next session
  whether any brochure prints an actual effective/revision date (LIC
  sometimes states "w.e.f. dd/mm/yyyy" near the UIN) rather than relying on
  UIN sequence as a proxy, then testing against a policy that's structurally
  similar to 876 but from a clearly different UIN vintage (if one exists in
  this corpus) to separate the two hypotheses.
