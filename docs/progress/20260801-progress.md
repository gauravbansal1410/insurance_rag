# Progress Log — 2026-08-01

Direct continuation of 2026-07-31's session — the full-corpus precompute job launched on the Oracle VM overnight, plus everything that followed once it finished.

## Precompute finished successfully overnight, on the second VM attempt

The full 7-policy x 8-tag precompute (launched fresh from zero on the
Oracle VM the night before, after two separate loss incidents — see
2026-07-31's log for the laptop-sleep and `systemd-logind` session-kill
root causes and fixes) completed on its own overnight: process exited
cleanly, log ended with `Done. 8 tags, 504 total (tag, chunk) rows`
(8 x 63 — exact match, no partial/missing data). Verified directly
before trusting the log: every one of the 8 tags covers all 7
policy_ids (`512N350V02`, `512N351V02`, `859`, `875`, `876`, `877`,
`878`) — the `setsid` + `loginctl enable-linger` fix held for the full
~2 hour run, well past the ~25-minute mark where the previous attempt
died silently.

Synced back: `scp`'d `chunking/precomputed_rerank_scores.json` from the
VM to the local clone (the VM has no push credentials at all, by
design — it only ever needs read access, so syncing this direction
avoids having to set up write credentials there), verified again
locally, committed and pushed as a single commit per the user's
explicit choice ("commit once at the end" over incremental syncing,
given the VM's own checkpointing already protects against data loss
there).

## Decided: keep precompute at 8 individual tags, not all 255 tag combinations

Raised by the user once the VM removed the "have to babysit it"
constraint: since it can now run unattended indefinitely, why not
precompute every possible `concern_tags` combination instead of the
8 tags individually, for an exact (not approximated) live-path lookup?

Recommended against it, for reasons that don't go away just because the
job runs silently:
- **Token budget, not wall-clock time, is the real constraint.**
  Reranking cost scales with documents x queries; 255 combinations
  against the same 63 chunks is ~255x one tag's cost - at this
  category's size alone, roughly ~25M tokens (~12.5% of Voyage's entire
  200M/month free allowance) for one precompute pass. At the full
  32-policy corpus this project is scoped to, a full-combination
  precompute would consume a large fraction of the *entire monthly*
  free budget, repeatedly, every time the tag vocabulary changes
  (`docs/schema.md`'s own checklist already anticipates that as a real,
  expected event, not hypothetical).
- **Doesn't scale as the tag set grows** - 8 tags -> 255 combos, 9 tags
  -> 511 combos, doubling with every new tag vs. the per-tag approach's
  linear growth. This was the actual original reason 8-tags-not-combos
  was chosen; moving to a non-sleeping VM doesn't change this math.
- **Not clearly even a quality improvement** - real data already shows
  rerank scores clustering very tightly for similar policies (one
  earlier test: 4 candidates within a 0.0078 spread), which the
  tolerance-band tiering already absorbs; there's also a real argument
  that blending unrelated concerns into one query *dilutes* signal for
  each individually, making max-over-tags not just cheaper but possibly
  more interpretable, not merely an inferior approximation.

Decision: staying at 8 tags. If more confidence in the approximation is
wanted later, a cheap spot-check of a handful of real combinations
against the max-over-tags result costs far less budget than doing all
255 for real.

## Verified: the full query pipeline works end-to-end for the first time, against complete real data

Restarted the local `qdrant-tunnel` and `uvicorn` (both stopped
overnight) and ran a full `/chat` conversation through to actual
completion for the first time against genuinely complete precomputed
data (previous attempts either used synthetic coverage or were
deliberately stopped short to avoid competing with the in-flight
precompute job). Full pipeline confirmed working: deterministic Q&A
slot-filling (all 6 questions, natural phrasing parsed correctly) ->
eligibility/budget filter -> precomputed lookup (zero Voyage calls,
zero missing policies this time) -> ranking (three candidates correctly
tied in one relevance tier, sorted by premium ascending) -> grounded
Gemini narrative generation with the disclosure paragraph reproduced
verbatim.

## Bug found and fixed: `plan_name` fallback didn't handle a `None` value, only a missing key

The real end-to-end test's narrative headings showed raw `policy_id`
values (`### 512N351V02`, `### 876`) instead of real plan names.
Investigated rather than assumed cosmetic: `layer1_record["layer1"].get(
"plan_name", candidate["policy_id"])` only falls back when the KEY is
absent - `plan_name` is present but **`None`** for all 7 term-assurance
Layer 1 records (checked directly, not assumed from one or two
samples), so `.get()` returned the literal `None`, meaning the prompt
sent to Gemini literally contained "Plan name: None". Earlier test runs
that showed correct plan names ("LIC's Yuva Term" etc.) only worked by
coincidence - Gemini occasionally pulled the real name from the
grounding document excerpts instead of the broken structured field,
not reliably.

Fixed: `.get("plan_name") or candidate["policy_id"]` - handles both the
missing-key and the present-but-None cases. Verified fixed on a second
full `/chat` run: headings now cleanly show `policy_id` as the intended
fallback (no literal "None" leaking into the prompt) rather than
crashing or producing garbage - not pretty, since the underlying data
gap remains, but no longer buggy.

**Not fixed, flagged for a future ingestion-focused session:**
`plan_name` is `None` for **all 7** term-assurance Layer 1 records, not
an isolated case - a real, universal extraction gap, given
`docs/schema.md` documents `plan_name` as a required top-level Layer 1
field (`plan_name, uin, plan_category`). Root cause not investigated
this session (deliberately, to stay focused on finishing the query
pipeline test) - worth a dedicated look at why the extraction
prompt/traps never populated a field this basic, likely affecting
every future category too if the same prompt pattern is reused.

## Also: a git-pull habit correction for the Oracle VM

User asked directly why the VM's clone doesn't stay in sync
automatically, prompting a distinction: (1) proactively `git pull` on
the VM before running anything there / after pushing changes meant for
it - a correctness habit (stale code there can silently reproduce
already-fixed bugs, as almost happened tonight), adopted as a standing
practice rather than infrastructure; (2) a cron-based fully-automatic
pull - offered, explicitly declined by the user for now ("just #1").
Saved (1) as a feedback memory.

## Open / not done

- `plan_name` is `None` across all 7 term-assurance Layer 1 records -
  root cause not investigated, needs a dedicated ingestion-side look.
- The actual n8n workflow (Chat Trigger -> HTTP Request -> Respond)
  still hasn't been built in n8n itself.
- `service/` (the FastAPI app) is still local-only - only the
  precompute *batch job* has ever run on the VM, not the query-serving
  service.
- BYOK (step 10) remains entirely unbuilt.
- Trace log (step 9) remains entirely unbuilt - both deliberately
  deferred per the user's explicit prioritization on 2026-07-31.
