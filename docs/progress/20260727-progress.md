# Progress Log — 2026-07-27

## Restructure: /extracted/ as the permanent Layer 1/2 store

Moved all 14 Layer 1/2 JSON files (7 policies × 2 layers) out of
`extraction_test/` into a new `/extracted/` at repo root, as their own
commit before touching any filter logic, so the file moves and new code
aren't tangled in git history. `extraction_test/` now holds only the
three pipeline scripts (`run_pipeline.sh`,
`run_layer1_extraction.py`, `run_layer2_derivation.py`); `/extracted/`
is the permanent, versioned store `docs/schema.md`'s "Curated knowledge
base" section already described in principle but didn't name a path
for. `run_pipeline.sh`'s output-dir default now points at
`/extracted/` (the explicit `[output_dir]` override still works).
Updated `CLAUDE.md`'s directory structure and `docs/schema.md` to match.

## query_architecture.md: steps 3-5 rewritten from design to implementation-ready

Design work happened in a separate claude.ai Project planning session;
this session translated it into concrete doc changes and code.

- **Step 3 (eligibility filter):** dropped the original "Qdrant payload
  filter" design in favor of an in-memory Python filter over Layer 2
  JSON loaded from `/extracted/`. No vector-search need exists for
  structured field filtering at this corpus's scale (32 policies max
  across all categories once fully extracted) — Qdrant stays scoped to
  Layer 3 narrative-chunk search only. Concern-tag matching (Group A)
  is now a hard gate (≥1 overlap required to pass), not just a sort
  input as originally designed. Riders are checked independently of
  their base plan — passing eligibility is never inherited.
- **Step 4 (fallback):** rewritten as an explicit two-tier relaxation.
  Tier 1 (<3 matches): relax Group C bounds only, concern gate stays
  firm. Tier 2 (still <3): also drop the concern gate. Budget is not
  relaxed at either tier, still step 5's job.
- **Step 5 (premium estimation):** replaced Gemini-based interpolation
  with deterministic two-point linear interpolation over Layer 1's
  `sample_illustrative_premiums`, matched within a single
  `premium_payment_option` column, scaled linearly for the profile's
  actual sum_assured against the table's baseline. Removed "whether
  Gemini can reliably interpolate premiums" from open questions (moot).
- Added a runtime-data-source note: the live query pipeline (n8n,
  Oracle VM) reads Layer 1/2 JSON from a local git clone kept current
  via manual end-of-session `git pull`, never a per-query GitHub raw
  fetch. Added the corresponding checklist item + credential-helper
  note to `docs/infra-baseline.md`.
- Added a new open question: Voyage's free-tier 3 RPM / 10K TPM cap
  (steps 6-7) is the real constraint on concurrent live-query
  throughput, not VM compute.

## Built and tested: query/ — steps 3-5 implementation

New `query/` directory, no test suite (matches project convention) —
each module's `__main__` block runs hand-checked profiles against real
`/extracted/` term-assurance data instead.

- `load_extracted.py`: loads all `/extracted/layer1_*.json` and
  `layer2_*.json` into memory, keyed by `policy_id` read from each
  file's JSON content — not parsed from the filename, since two files
  carry historical non-canonical names
  (`layer1_859_flashlite_pdf_v2_run1.json`,
  `layer1_877_flashlite_pdf_v3_run1.json`) from earlier test runs.
- `eligibility_filter.py`: `filter_eligible()` does the Group C bound
  checks (null-safe on `max_sum_assured` for the "No Limit, subject to
  underwriting" case) plus the concern-tag gate; `apply_fallback()`
  wraps it with the two-tier relaxation. Verified against real data:
  a realistic profile (age 30, SA 50L, term 20, income_replacement)
  clears tier 0 cleanly against 4 policies; dropping SA to 10L forces
  tier 1, since most term plans in this corpus have a 50L minimum
  (only 859 goes as low as 5L); an out-of-range age (5) also forces
  tier 1; a concern tag no term policy carries (retirement_income)
  correctly falls through to tier 2 with `concern_match_count: 0`
  across all 7.
- `premium_interpolation.py`: `interpolate_premium()` does the
  two-point interpolation + SA scaling; `filter_by_budget()` applies
  it to eligibility survivors and excludes-and-logs anyone over budget
  or outside a policy's sample-table range. Verified against 877's
  clean `limited_ppt_15`/term-25 column: exact-age match, true
  age-interpolation (midpoint of two real table values), and SA
  scaling all matched hand-calculated values from the raw table.

## Data-quality finding: ambiguous sample-premium rows on 4/7 term policies

While hand-verifying `interpolate_premium()`, found that 4 of the 7
term-assurance Layer 1 files (875, 876, 954, 955) contain exact
duplicate rows in `sample_illustrative_premiums` — identical on every
currently-extracted field (age, term, `premium_payment_option`,
`payment_mode`, `distribution_channel`, `sum_assured`) but with two
different `premium_amount` values. E.g. policy 875's age-30/term-20/
regular row exists twice: once at 5950, once at 8250. Nothing in the
current schema distinguishes them.

Root cause not confirmed, but the most likely explanation is an
uncaptured schema dimension — gender-split premium tables are common in
LIC brochures, and neither `docs/schema.md`'s
`sample_illustrative_premiums` field list nor the extraction prompt
models gender anywhere. (Notably, the query pipeline's own profile
collection in step 1 already asks for "smoker status" but not gender —
worth checking whether that's the actual differentiator instead, once
this is investigated properly.)

Rather than silently pick one of the two values — which would produce
a wrong premium quote for a real user — `interpolate_premium()` now
detects this case (`_ambiguous_ages()`) and hard-excludes with a
flagged reason, verified via `layer1["875"]`'s age-30/term-20/regular
combo. 877 and 878 have no such duplicates and were used for the clean
interpolation/scaling test cases instead.

**Not fixed in that pass** — root cause identified, but the fix landed
as a same-session follow-up (below) rather than a hand-patch, once the
actual explanation came from checking the raw PDFs directly.

## Root cause + real fix: level vs increasing sum assured (same session, follow-up)

User checked the raw PDFs directly and found the actual explanation:
the "ambiguous" duplicate rows aren't a gender split (my first guess) —
they're two full sample-premium tables for two different death-benefit
structures, "Level Sum Assured" (fixed cover throughout the term) and
"Increasing Sum Assured" (cover grows over the term). Confirmed the
positional-split heuristic I'd initially patched into
`premium_interpolation.py` was the wrong layer to fix this at: it
infers "level = whichever table came first," which nothing in the
schema or prompt actually guarantees, and would silently mislabel data
if that ordering ever didn't hold. Fixed at the source instead:

- `docs/prompts/prompt_a_pdf.txt`: new trap 16 — extract
  `sum_assured_type` (`"level"` | `"increasing"`) per premium row, read
  from the document's own table heading, never inferred from table
  order or position. Defaults to `"level"` when a document offers no
  such choice.
- `docs/schema.md`: documented the new field in both the
  `sample_illustrative_premiums` row shape and the extraction-rule
  caveats list.
- Re-ran Layer 1 extraction for the 4 affected policies (875, 876, 954,
  955) with the updated prompt. Verified before overwriting committed
  data: `sum_assured_min/max`, age/term bounds, `policy_id`, and the
  full set of `premium_amount` values all unchanged from the previously
  committed extraction — only `sum_assured_type` is new. 875 clean on
  the first attempt; 954/955 clean on the first attempt; 876 needed 3
  attempts (see reliability note below).
- `query/premium_interpolation.py`: `_split_sum_assured_variants()`
  rewritten to group by the real `sum_assured_type` field instead of
  the positional guess. All hand-checked values unchanged (875
  level=5950/increasing=8250, same as the heuristic version) — same
  answers, no longer inferred.
- `docs/query_architecture.md` step 5: documented `sum_assured_type` as
  a second interpolation-matching dimension alongside
  `premium_payment_option`.

**Scope check on the other 3 term policies (877, 878, 859):** re-
extraction only targeted the 4 policies that showed the duplicate-row
symptom — a reactive check, not a proactive audit of the full category.
Confirmed via direct text search (`grep -i "increasing sum assured\|level
sum assured"`) that neither phrase appears anywhere in 877/878's or
859's source PDFs (policy_doc or brochure) — 877/878 are credit-life
plans with a decreasing, loan-linked cover (a different mechanism
entirely, not level/increasing), and 859 is a simple single-structure
term plan. So these 3 genuinely have no level/increasing split to miss,
not just "no duplication found, assumed clean."

**Reliability finding, not fixed:** re-extracting 876 got
`sum_assured_max` wrong (500,000,000 instead of the correct 50,000,000,
i.e. brochure's "Rs. 5,00,00,000") on 2 of 3 attempts — confirmed wrong
against the raw brochure PDF, unrelated to the `sum_assured_type` trap.
The model appears to intermittently misread this specific Indian
digit-grouping format ("5,00,00,000"). The 3rd attempt was correct and
is what's committed, but the underlying flakiness on this number format
isn't fixed and likely affects other policies with similar figures —
worth a dedicated look in a future session.

## Open / not done

- 876's Indian-digit-grouping misread on `sum_assured_max` (2/3
  extraction attempts wrong) — root cause not investigated, no prompt
  fix yet. Worth checking whether other policies' large Sum Assured
  figures are similarly at risk.
- Steps 1-2 (frontend slot-filling, session state) and 6-10 (narrative
  retrieval, reranking, generation, trace log, BYOK) are still
  design-only — today's session covered steps 3-5 only.
- VM-side `git pull` sync and its credential-helper setup are
  documented in `docs/infra-baseline.md` but not yet actually
  performed/verified on the Oracle VM.
- Query pipeline steps 3-5 are only exercised against term-assurance
  data (7 policies) — untested against any other category, none of
  which are extracted yet.
