# Progress Log — 2026-08-10

Direct continuation of 2026-08-08 (three premium lookups built - 876, 859, 875 -
and a first real shape-transfer finding: 875 tracks 876 closely, 859 doesn't).
This session started premium lookups for the remaining 4 term-assurance policies
(877, 878, 954, 955) via a spot-check-first strategy informed by that finding,
rather than defaulting straight to full 30-50 point scrapes.

## Confirmed chunking/embedding/precompute did NOT need re-running

User asked to redo Layer 3 chunking, embedding, and precompute rerank scores,
since Layer 1/2 got re-extracted for 5 policies on 2026-08-08. Checked the
actual dependency chain before doing any work: `chunk_policy_doc.py` reads the
raw `policy_doc.pdf` directly, `embed_and_load_layer3.py` reads the chunking
output, and `precompute_rerank_scores.py` reads those same chunks plus the
fixed 8 `concern_tags` - none of these three scripts touch Layer 1/2 JSON at
all. Since the raw PDFs never changed (only re-ran Gemini extraction calls
against PDFs already in `raw_pdfs/`), the existing chunks/embeddings/precompute
are still built from identical source text and remain valid. Verified directly:
`chunking/chunks_*.json` still has all 7 policies, and
`chunking/precomputed_rerank_scores.json` still shows 504/504 (8 tags x 63
chunks, all 7 policy_ids present per tag). Saved a real multi-hour Voyage job
for no reason - would have been pure waste.

## Designed a spot-check-first strategy for 877/878/954/955

Rather than 4 more full grids, proposed scraping ~6-8 points per policy first
and comparing against 875/876/859's already-known shapes, informed directly by
2026-08-08's finding that shape transfers within a product family but not
across one. User confirmed: spot-check for both pairs (877/878 despite having
no existing credit_life baseline to compare against, mainly to get a data
point on what predicts transfer; 954/955 since they share 859's wide age
range).

Domain bounds confirmed against already-extracted Layer 1 (877/878: entry_age
18-45, term 5-30, `age_at_maturity_max` 75; 954/955: entry_age 18-65, term
10-40, `age_at_maturity_max` 80). Points chosen to overlap with 876/875/859's
existing anchors for direct comparability, and to reuse each policy's own
brochure sample points as free anchors rather than re-scraping them.

## 877: real premium_payment_option mistake caught by the user, corrected

Specified "regular" premium for 877/878 in the scrape list - wrong. User
flagged (via a live screenshot) that 877's Coverage page only offers Single or
Limited Premium, no Regular at all - which the project already knew (this is
literally the `premium_payment_options` bug fixed in 877's 2026-08-08
re-extraction, `["single","limited"]`, no `"regular"`). Should have caught
this myself before specifying the scrape. Corrected to Limited Premium,
PPT=10 (valid under both terms 20/25, matches a tier 877's own brochure
already offers), Interest Rate Loan=8% (matching the rate 877/878's own
brochure sample-illustrative-premium tables use, so new scrape points stay
comparable to the free brochure anchors).

**All 6 points scraped and saved to `877_scraped_premiums.csv`.** One CSV bug
caught and fixed immediately (again) - a comma inside an unquoted notes field
produced 17-field rows instead of 16; this is the second time this exact
mistake has happened in this project (first was 859's cross-SA rows,
2026-08-08) - worth remembering to avoid commas in notes text entirely, not
just catching it after the fact each time. Row 4 (age 30/term 25) matched
877's own brochure sample exactly (Rs 7,900), a strong confirmation the
PPT/interest-rate assumptions were right.

## 878: a second real discrepancy, also correctly resolved by checking the actual document

878's Coverage page pre-selected Sale Type = Online, no Offline option shown -
opposite of 877 (Offline only). Not a bug or inconsistency to work around:
878 ("Digi Credit") is LIC's online-only product line, confirmed directly
against its own brochure text ("This plan shall be available Online only and
can be purchased directly through website www.licindia.in") - same pattern
876 (Digi Term) already follows. Confirmed Online was correct, not a
deviation from the plan. All 6 points scraped and saved to
`878_scraped_premiums.csv`, same PPT/interest-rate assumptions as 877. Row 4
(age 30/term 25) again matched the brochure's own printed sample exactly
(Rs 6,250).

## Shape-transfer analysis extended with two credit_life data points

With both 877 and 878 complete (same `structural_variant: credit_life`,
scraped under identical Limited/PPT10/8% assumptions), ran three
comparisons:

1. **877 vs 878 (same family, same payment-option assumptions)**: 0.1-2.7%
   deviation across every point checked - as tight as 875-vs-876's earlier
   result. Their absolute premiums also sit in a near-constant ~1.24-1.28x
   ratio at every point (877 consistently pricier than 878) - a pure level
   shift, not a shape difference.
2. **877 vs 876/875 (different family)**: 9-19% divergence, widening with
   age (9% at age 30, 17-19% at age 45).
3. **878 vs 876/875 (different family, no payment-option confound relative
   to 877's own comparison)**: 15-19.6% divergence at age 45, closely
   matching 877's own divergence magnitude (877: -19.3%/-18.3%,
   878: -17.6%/-16.0%) despite 877 and 878 differing in sale channel
   (offline vs online) and branding (Yuva vs Digi).

**Key inference: since 877 and 878 both diverge from 876/875 by nearly the
same amount despite using identical payment-option assumptions to each
other, this divergence is very unlikely to be primarily a Limited-vs-Regular
payment-structure artifact - it looks like a real, repeatable
`structural_variant` (credit_life vs standalone) effect.** Still can't fully
rule out a general Regular-vs-Limited effect (no regular-premium credit_life
data exists to test that in isolation), but two independent credit_life
products landing on nearly the same divergence number is real evidence, not
noise.

**Updated summary table** (logged in `docs/query_architecture.md`'s open
questions):

| Pair | Divergence at oldest age checked | Same structural_variant? |
|---|---|---|
| 875 vs 876 | 0.6-2.4% | Yes (standalone) |
| 877 vs 878 | 0.1-2.7% | Yes (credit_life) |
| 877/878 vs 876/875 | 15-19% | No |
| 876 vs 859 | 30-33% | No, and different regulatory template too |

Suggestive pattern: divergence scales with how different the two policies'
families actually are - same-family pairs transfer tightly regardless of
other differences (channel, branding, absolute price level); cross-family
divergence is moderate when only `structural_variant` differs
(credit_life vs standalone) and much larger when the products are also from
different regulatory templates (859 is a standardized cross-insurer product).

## Docs updated
`docs/query_architecture.md` (shape-transfer open question extended with the
877/878 data and the summary table), `docs/schema.md` (ground-truth CSV
section - `877_scraped_premiums.csv`/`878_scraped_premiums.csv` added).

## Open / not done
- 877/878's actual premium lookups (age x term grid + `POLICIES` entries in
  `build_premium_lookup.py`) not yet built - only 6 spot-check points each,
  not a full grid. Given the confirmed moderate divergence from 876/875 (they
  can't borrow those curves), and no other credit_life baseline exists yet,
  each still needs its own fuller scrape to build a real lookup - the
  spot-check told us "don't skip scraping for these," not "here's the curve."
- 954/955 spot-check not started yet - domain bounds and point lists already
  prepared (2026-08-08/2026-08-10), not yet run.
- The `structural_variant`-vs-payment-option confound isn't fully resolved -
  would need a real regular-premium credit_life comparison point (doesn't
  exist anywhere in this corpus) to fully separate the two effects. Flagged,
  not blocking.
- The user's other open hypothesis from 2026-08-08 (launch/revision vintage
  vs. structural similarity, UIN-based circumstantial check) is still
  untested - unrelated to this session's work.
