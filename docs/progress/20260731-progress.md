# Progress Log — 2026-07-31

## Built: query/ — step 6 (narrative retrieval)

New `query/narrative_retrieval.py`. Qdrant vector search over Layer 3
chunk-level records, filtered to only the `policy_id`s that survived
steps 3-5 (`Filter`/`FieldCondition`/`MatchAny` on `policy_id`, not an
independent global search) — Qdrant does semantic search only inside an
already-narrowed set, per `docs/query_architecture.md` step 6.

- `synthesize_query_text()`: deterministic, no-LLM synthesis of the
  user's Group A `concern_tags` (the fixed 8-tag set, no "other"
  catch-all — `docs/schema.md`) into one natural-language query string
  via a fixed tag -> phrase mapping. Keeps step 6 as fast/deterministic
  as steps 3-5, no LLM call.
- `embed_query()`: embeds that query string with `voyage-law-2` using
  `input_type="query"` — deliberately not `"document"` (which
  `embed_and_load_layer3.py` used for the chunks). Voyage's asymmetric
  embedding scheme means query and document text are embedded
  differently for retrieval; using the wrong `input_type` silently
  degrades match quality rather than erroring, so this was called out
  explicitly rather than left to be discovered later.
- `retrieve_narrative_chunks()`: embeds the query, then
  `qdrant_client.query_points()` filtered to the surviving
  `policy_ids`, `limit` ~15-20 per the doc.
- Verified against live data (tunnel + real `insurance_rag_layer3`
  collection): correct filtering (an out-of-corpus `policy_id` returns
  0 hits, confirming the filter is actually enforced, not just
  returning everything); real cosine-similarity scores clustered
  tightly (0.42 → 0.38) across all hits for 3 term-assurance policies —
  expected, since similar plans share similar language, and exactly why
  step 7's rerank + `concern_match_count` tiebreak exist rather than
  relying on raw vector similarity alone.

## Built: query/ — step 7 (reranking + sort)

New `query/rerank_and_sort.py`. Voyage `rerank-2.5-lite` re-scores step
6's retrieved chunks for semantic relevance; results are then sorted
per `docs/query_architecture.md` step 7 — not one blended score:

1. `concern_match_count` (step 3) descending, primary key.
2. Within a `concern_match_count` block, group into relevance tiers by
   rerank score, using the doc's placeholder tolerance band (0.05,
   still uncalibrated).
3. Within a tier, sort by interpolated premium (step 5) ascending.

Implementation notes:

- Rerank scores are per-chunk; `concern_match_count`/premium are
  per-policy. `max_rerank_score_by_policy()` collapses to one score per
  policy by taking the max across that policy's retrieved chunks — a
  policy's relevance is judged by its single most relevant passage, not
  penalized for having some boilerplate sections.
- `_assign_tiers()`: the doc's "candidates within a tolerance band of
  each other are tied" isn't transitive (A~B, B~C doesn't imply A~C).
  Resolved with the simplest defensible rule — sort descending, cut a
  new tier whenever the gap between *consecutive* scores exceeds the
  tolerance — rather than inventing a chaining/clustering rule the docs
  don't specify.
- `rank_candidates()` flags rather than silently drops candidates that
  survived steps 3-5 but got no chunk back from step 6's retrieval
  (`rerank_score: None`, sorted last within their `concern_match_count`
  block) — same "flag, don't silently guess" pattern used elsewhere in
  this codebase (e.g. `premium_interpolation.py`'s ambiguous-row
  exclusion).

## Data-quality/behavior finding: identical rerank scores across similar policies

Hand-verifying step 7 against real data, 875 and 876 (both
term-assurance) got the *exact same* rerank score to 6+ decimal places
for their top-matching chunk. Confirmed via direct Qdrant scroll this
isn't a batching/index bug — their `PART_B` (definitions) and `PART_D`
("Proof of Age") sections are genuinely near-identical boilerplate
LIC legal language. Worth remembering for step 8 (narrative
generation): the reranker will systematically undervalue
differentiation between similar policies whose `PART_B`/`PART_D`
sections are generic — the more useful differentiating content likely
lives in `PART_C` (benefits).

## Bug found + fixed: Voyage rerank calls need the same batching as embeds

`rerank_chunks()` originally sent all of step 6's retrieved chunks'
full text in a single `rerank()` call. Confirmed 2026-07-31 that 20
full-section chunks (~25.8K words) is ~33.5K estimated tokens — over
3x Voyage's free-tier 10K TPM cap in one request. Retrying with backoff
can't fix a single request that's already over the limit by itself,
only a transiently-full window.

Fixed by batching the same way `embed_and_load_layer3.py` already does
(greedy word-budget batching, `MAX_BATCH_WORDS=5000`, batches paced
`SECONDS_BETWEEN_BATCHES=65` apart), with per-batch retry-with-backoff
on `RateLimitError` (`MAX_RETRIES=3`). `rerank_chunks()` now returns
plain `{"chunk_index": i, "relevance_score": ...}` dicts with indices
already remapped across batches, rather than Voyage's own
per-batch-relative `RerankingObject.results[].index`, which would only
make sense within one batch's document list.

## Reliability finding, not a bug: apparent "hangs" were rate-limit backoff, not stuck code — but the "15+ minutes" estimate itself was wrong

While verifying the batching fix, a debug script and later the real
`rerank_and_sort.py` `__main__` run both appeared to hang with 0% CPU
and no output past step 6, estimated at the time (by counting
conversation turns / scheduled-wakeup intervals, not a clock) at 9-15,
then 30+, minutes. Spent real time diagnosing this as a suspected
hang (added `REQUEST_TIMEOUT_SECONDS=30` to both `QdrantClient` and
`voyageai.Client` as a real fix regardless — neither client raised
promptly on a stalled connection without one, which is worth keeping
independent of the rest of this finding).

**Correction:** the elapsed-time estimates driving that whole
investigation were wrong, confirmed by checking `ps -o lstart,etime`
against actual wall-clock time instead of counting turns/wakeups — a
run believed to be 30+ minutes in was, per `ps etime`, only ~3.5
minutes in. Turn-counting/scheduled-wakeup-counting is not a reliable
proxy for real elapsed time and shouldn't be used to diagnose
hangs/timeouts again — always check `date`/`ps etime` directly.

With that corrected, the actual, clock-verified behavior: a live run
against the real 4-policy/20-chunk profile crashed via a legitimate,
correctly-raised `RateLimitError` after **6m34s** real time (process
`lstart` 16:57:53 -> crash at 17:04:27) — not a hang, the retry loop
worked exactly as designed (3 attempts x 65s backoff on one batch,
exhausted, correctly re-raised rather than looping forever). The
account was still under rate-limit contention from earlier ad-hoc
debug `rerank()`/`embed()` calls made during the same investigation
(which kept Voyage's rolling 60s TPM window near capacity), so 3
retries wasn't enough headroom to outlast it. Fixed by bumping
`rerank_and_sort.py`'s `MAX_RETRIES` from 3 to 6 (more wall-clock
patience, no cost) rather than by adding a timeout (that part was
already fine — the process was correctly retrying and correctly
crashing, just running out of attempts too soon).

Lesson for future debugging on this project: (1) don't make
parallel/exploratory Voyage calls while a real batched pipeline run is
also in flight — it competes for the same rate-limit window; (2) when
judging whether something is stuck, check `ps etime`/`date`, never
estimate from how many turns or scheduled wakeups have passed.

## Also fixed: qdrant-tunnel alias hangs when run non-interactively

Running the `qdrant-tunnel` alias (`ssh -i ... -L 6333:localhost:6333
ubuntu@...`, no `-N`) via a backgrounded, non-interactive shell opens a
login shell that immediately hits EOF on stdin and exits, tearing down
the port forward with it — worked fine typed into a real terminal, not
when launched programmatically. Fixed for this session by running
`ssh -i ~/.ssh/claude_code_oracle -N -L 6333:localhost:6333
ubuntu@68.233.108.134` directly (`-N`: forward only, no remote shell,
so it doesn't need a tty and stays up). Not yet changed at the alias
level in `~/.zshrc` — worth doing if non-interactive tunnel starts
become routine.

## Refactor: shared client construction pulled out of narrative_retrieval.py / rerank_and_sort.py

Both modules' `__main__` blocks were independently constructing
`voyageai.Client`/`QdrantClient` from env vars with the same
`REQUEST_TIMEOUT_SECONDS`, duplicating client-config concerns that
don't belong to "retrieve chunks" or "rerank + sort" as modules (their
actual functions already just take client instances as parameters —
that separation was already correct). Pulled into new
`query/clients.py` (`make_voyage_client()` / `make_qdrant_client()`);
both modules' `__main__` blocks now just import and call those.

## Bug found + fixed: 954/955 chunked under the wrong policy_id, invisible until step 6

The `rerank_and_sort.py` verification run above flagged
`512N351V02`/`512N350V02` as unscored (no chunk retrieved). Investigated
rather than assumed-benign: a direct Qdrant scroll on those `policy_id`s
returned zero points, not just low-ranked ones.

Root cause: `extracted/layer1_954.json`'s own JSON content declares
`policy_id: "512N351V02"` (the real LIC UIN) — Layer 1/2 loading keys
everything by this field (`load_extracted.py`), which is why steps 3-5
correctly surface `"512N351V02"` as the survivor. But Layer 3 chunking
was invoked as `chunk_policy_doc.py 954 term_assurance ...`, passing the
numeric filename id as the CLI `policy_id` argument instead of the UIN
— so `chunking/chunks_954.json` and the resulting Qdrant points were
tagged `policy_id: "954"`, a value that appears nowhere in Layer 1/2.
Same story for 955 -> `512N350V02`. The other 5 term-assurance policies
(859, 875, 876, 877, 878) use the plain numeric id consistently at
every layer, so this only affected 954/955 and was invisible until step
6 tried to join across layers by `policy_id` for the first time.

Fixed: re-ran `chunk_policy_doc.py` for both, passing the correct UIN
(`512N351V02` / `512N350V02`) against the same source PDFs
(`raw_pdfs/term_assurance_plans/policy_doc_954_...pdf` /
`..._955_...pdf`) -> `chunking/chunks_512N351V02.json` /
`chunks_512N350V02.json` (9 chunks each, same section set as before).
Deleted the 18 mistagged `policy_id="954"/"955"` points from Qdrant
first (`FilterSelector` delete), then re-embedded and upserted the
correctly-tagged chunks via `embed_and_load_layer3.py`. Verified via
Qdrant scroll: `512N351V02` and `512N350V02` each have 9 chunks under
the right id; collection total is back to 63 (7 policies x 9 sections).
The old `chunking/chunks_954.json`/`chunks_955.json` files are now
stale (superseded by the UIN-named ones) but not yet deleted from the
repo — worth cleaning up in a follow-up commit rather than leaving two
policy_id conventions on disk for the same two policies.

## Open / not done

- Step 7's rerank-score tolerance band (0.05) is still the doc's
  placeholder — not calibrated against a real rerank score
  distribution yet, though we now have real scores from live runs to
  eventually calibrate against.
- The final `rerank_and_sort.py` end-to-end verification run against
  live data completed successfully after the clean, non-interfered-with
  attempt: 876 and 875 tied on relevance (same tier, rerank_score
  0.5234375 each — see the boilerplate finding above) and were
  correctly ordered by premium ascending within that tie (876 at 4700
  before 875 at 5950); 512N351V02/512N350V02 were correctly flagged
  (`rerank_score: None`) and sorted last at the time, since their
  chunks didn't exist in Qdrant yet — root cause found and fixed
  same-session (see above). Re-run against the corrected data: step 6
  now correctly retrieves chunks across all 4 policies (confirmed).
  Step 7's rerank call then crashed twice more on legitimate,
  correctly-raised `RateLimitError`s (once after exhausting the
  original `MAX_RETRIES=3`, again after bumping to 6 — the account was
  under sustained rate-limit pressure from the sheer volume of Voyage
  calls made across this session's debugging, not a code defect) before
  finally succeeding on a third attempt, after an explicit multi-minute
  cooldown with zero Voyage calls and a shrunk retrieval `limit` (20 ->
  10, fewer total chunks/words -> fewer rerank batches needed). Final
  confirmed result, all 4 candidates now scored (none flagged
  unscored): `512N351V02` (954) 0.4727/4567.5, `876` 0.4668/4700.0,
  `875` 0.4648/5950.0, `512N350V02` (955) 0.4688/7830.0 — all 4 land in
  one relevance tier (spread 0.464-0.473, within the 0.05 tolerance),
  so final order is premium ascending: 954 -> 876 -> 875 -> 955.

## Design concern raised: is Voyage's free tier viable for the live query path at all?

Watching real query-time latency during this verification (a floor of
several minutes per query even in the best case, once step 6 needs its
full ~15-20 chunks rather than this session's deliberately-shrunk
10-chunk test) raised a real question: `embed_and_load_layer3.py`'s
"expect minutes, not seconds" pacing was designed for one-time,
admin-triggered ingestion — that assumption does not transfer to a
live, user-facing query path, and this is the first session that
actually exercised steps 6-7 end-to-end for real to surface it. At
typical chunk sizes (~800-2200 words/section measured this session),
step 6's documented "~15-20 chunks" is commonly 15-25K words, needing
4-5 rerank batches at a mandatory 65s spacing each — a several-minute
floor on *every* query, not a contention-driven worst case. Already
flagged as an open question in `docs/query_architecture.md` ("the real
constraint on concurrent live-query throughput... worth revisiting
past solo personal use") but the severity (multi-minute baseline
latency, not a throughput ceiling under load) wasn't previously
demonstrated. Not resolved this session — options discussed: add a
Voyage payment method (lifts the RPM/TPM ceiling; free 200M
tokens/month still apply per Voyage's own error text, so may not
violate the project's $0-recurring constraint at this scale, but
unverified against Voyage's actual paid-tier limits/pricing); rerank
only one representative chunk per surviving policy instead of top-15-20
across all of them (keeps a single rerank call within one free-tier
request, no batching needed at query time); or drop step 7's rerank
call entirely for this corpus size (876/875 scored identically to 6
decimal places in this session's testing, suggesting rerank's
discriminative value here may not be worth its cost). Needs a decision
before steps 6-7 can be considered production-ready for a live query
path, independent of their logic already being verified correct.
- Stale `chunking/chunks_954.json`/`chunks_955.json` (wrong policy_id)
  not yet deleted from the repo.
- `qdrant-tunnel` alias itself not yet updated to include `-N` for
  non-interactive use (see fix note above).
- Steps 1-2 (frontend slot-filling, session state) and 8-10 (narrative
  generation, trace log, BYOK) are still design-only.

## Built: offline-precomputed reranking, replacing a live Voyage rerank call at query time

Direct response to the Voyage-viability finding above. Since step 6-7's
relevance signal depends only on the user's `concern_tags` (a fixed,
closed 8-tag set, `query/concern_tags.py` — extracted from
`narrative_retrieval.py`, which now imports it instead of duplicating
it) and never on the user's numeric profile fields (age/sum_assured/
term, which only drive steps 3-5's eligibility filter), relevance can
be precomputed once, offline, independent of any specific user:

- `chunking/precompute_rerank_scores.py`: reranks every chunk passed in
  against each of the 8 fixed tags *individually* (not all 255 possible
  tag combinations — reranking the full corpus against every
  combination would take days at free-tier pace; 8 tags is tractable
  and grows only linearly as policies are added). Reuses
  `rerank_and_sort.py`'s batching/retry logic directly via a small
  `_ChunkAdapter` (wraps a plain chunk dict in a `.payload` attribute,
  matching Qdrant's `ScoredPoint` shape `rerank_chunks()` expects) —
  deliberately did not change `rerank_chunks()`'s already-validated
  interface. Output: `chunking/precomputed_rerank_scores.json`, keyed
  `tag -> chunk_id -> {policy_id, score}`, merged with any existing
  file by `chunk_id` (idempotent, same pattern as the Qdrant upsert).
- `query/precomputed_relevance.py`: the live-path lookup —
  `lookup_relevance_by_policy()` takes the user's `concern_tags` and
  the policy_ids surviving steps 3-5, returns a per-policy score as the
  **max** precomputed score across the selected tags and that policy's
  chunks. Zero Voyage calls, no query-time latency. Also returns which
  policy_ids aren't covered by the table at all, for a fallback to a
  live (cheap-only) rerank — not built this session, the lookup and its
  missing-policy signal are.
- **Flagged approximation, not hidden:** combining multiple selected
  tags via max-over-independently-scored-tags is not identical to
  `narrative_retrieval.py`'s live path, which joins tags into one query
  string and reranks against that. Documented as a deliberate
  approximation (in the module docstring, `docs/schema.md`, and
  `docs/query_architecture.md`) rather than presented as equivalent —
  mirrors an approximation already accepted elsewhere in this design
  (`max_rerank_score_by_policy` already collapses per-chunk scores to
  per-policy via max, not an average).

**Test run (1 policy, 875, 9 chunks × 8 tags):** launched in the
background to validate the new modules mechanically before committing
Voyage-call budget to the full 7-policy/63-chunk corpus — real-time
progress confirmed via `ps etime` this time (see the timing-methodology
correction above), not turn-counting.

## Built: chunking/ingest_policy.sh — one orchestration script for new-policy ingestion

Directly requested to prevent the 954/955 policy_id bug class from
recurring: chains Layer 1+2 extraction -> Layer 3 chunking -> embedding
-> precompute in one command, so no step can be manually forgotten or
run with a mismatched identifier. The specific fix: it takes a
`<file_id>` argument (used only to name Layer 1/2's output files,
matching `run_pipeline.sh`'s existing convention) and, after Layer 1
completes, **re-reads the real `policy_id` from Layer 1's own JSON
output** before invoking chunking/embedding/precompute — never assumes
`file_id` equals the canonical `policy_id`. That cross-check is exactly
what was missing when 954/955 were chunked manually earlier: nothing
forced the numeric filename convention and Layer 1's actual extracted
value (their real `policy_id` is the LIC UIN) to be compared.

## Docs updated to match today's session (precompute + query pipeline now built)

Found `README.md` and `docs/pipeline-flow.html` were stale independent
of today's precompute work — both still described query pipeline steps
3-7 as "designed, not yet built," when they were actually built and
verified in earlier sessions this week. Fixed as part of this session's
doc pass rather than left for later:

- `docs/query_architecture.md`: new "Reranking data source" section
  (precompute-first design, fallback, the max-over-tags approximation);
  step 7 rewritten; open questions updated (rerank tolerance now has
  real data to calibrate against — one live test's 4 candidates landed
  within a 0.0078 spread).
- `docs/ingestion_architecture.md`: new step 7 (precompute), "Running
  the pipeline" section now leads with `ingest_policy.sh`.
- `docs/schema.md`: new "Layer 3 — precomputed rerank scores" section
  (schema + the approximation), new bullet in "Curated knowledge base."
- `CLAUDE.md`: Build & Run Commands restructured around
  `ingest_policy.sh` as the recommended path, individual-stage commands
  kept as a sub-list.
- `README.md`: Status/How it works/Quickstart/Documentation sections
  corrected to reflect steps 3-7 as built, not designed; precompute
  added as pipeline step 4.
- `docs/pipeline-flow.html`: query pipeline steps 3-7 flipped from
  `built: false` to `true` with real verified examples (replacing
  placeholder/hypothetical numbers with actual run data from this
  week's sessions); new ingestion step 6 (precompute) added.
- `docs/infra-baseline.md`: checked, no change needed — the VM's
  existing `git pull` end-of-session sync already covers new repo files
  like `precomputed_rerank_scores.json` automatically.

## Built: check_concern_tags_sync.py + a documented checklist for changing the tag vocabulary

Raised directly by the user after walking through what changing a
concern_tag would actually require: the 8-tag vocabulary is duplicated
by necessity across `docs/prompts/prompt_b.txt` (what Gemini may
assign), `query/concern_tags.py` (the live-path canonical set),
`chunking/precomputed_rerank_scores.json`'s tag keys, and already-
extracted Layer 2 data — the same "manual, easy-to-forget re-sync
across files" shape as the 954/955 `policy_id` bug.

Considered building a full refresh script (auto re-derive Layer 2 +
re-run precompute), but explicitly declined by the user given today's
6-policy precompute run alone took ~2 hours at Voyage's free-tier pace
— an automatic trigger on every tag edit wasn't worth that cost.
Landed on: document the checklist directly in `docs/schema.md` next to
the Group A tag list (not buried in a progress log), plus a **read-only
check**, not an auto-fixer:

- `check_concern_tags_sync.py` (repo root): compares
  `query/concern_tags.py`'s tag set against `prompt_b.txt`'s declared
  list, `precomputed_rerank_scores.json`'s tag keys, and every
  `extracted/layer2_*.json`'s actually-assigned tags (flagging any tag
  used in already-extracted data that's no longer in the canonical
  set). Entirely local file reads/regex/JSON parsing — no Gemini or
  Voyage calls, so it's free to run anytime, not just after a tag
  change. Verified both the positive case (current 8-tag state reports
  in sync) and the negative case (a simulated fake tag-set mismatch, via
  monkeypatching in a throwaway check rather than editing real files,
  correctly reported the drift in both directions).
- Wired into `chunking/ingest_policy.sh` as a final step 5/5, per a
  follow-up question about who/when should run it: the checklist-driven
  manual case (after a deliberate tag change) is the main trigger, but
  running it for free after every new-policy ingestion too catches a
  freshly-derived Layer 2 assigning an out-of-vocabulary tag right when
  it's easiest to trace back to that specific ingestion run — a hard
  failure, not a warning, matching the script's existing verification
  steps (renumbered its echo'd step labels 1/4→1/5 etc. accordingly).
- `docs/schema.md`'s Group A section, `docs/ingestion_architecture.md`,
  and `docs/pipeline-flow.html` updated to describe both the manual
  checklist and the automatic ingestion-time trigger.

## Ran: full-corpus precompute for the remaining 6 term-assurance policies

`chunking/chunks_875.json` was precomputed as the 1-policy validation
test earlier in this session (see above); launched the remaining 6
(859, 876, 877, 878, 512N351V02, 512N350V02 — 54 chunks total, ~58.5K
words) in one background run once the test confirmed the mechanism
worked. Estimated ~96 batches at 65s pacing minimum (~1h45min+), in
the same "admin-time, expect hours not minutes at this scale" territory
already accepted for embedding a full category. Still running as of
this log entry — real elapsed time tracked via `ps etime`/`date`
throughout, not turn-counting, per the timing-methodology correction
earlier in this log.

## Commits made and pushed this session

1. `3590796` — query pipeline steps 6-7 (narrative retrieval, rerank + sort)
2. `f24eb9a` — 954/955 policy_id fix (chunking)
3. `2b587fd` — `ingest_policy.sh` orchestration script
4. `75ec84d` — doc updates for steps 6-7/precompute/`ingest_policy.sh`
5. `8dfaad0` — this progress log (initial version)
6. `7ded7fd` — offline-precomputed reranking (concern_tags.py, precompute + lookup modules, 1-policy test data)
7. `a837b4a` — concern_tags sync check + checklist
8. `b3e4c26` — wired sync check into `ingest_policy.sh` + doc updates

Split deliberately into small, single-purpose commits per explicit
instruction — mirrors this repo's own established pattern (e.g. the
2026-07-27 session already split a doc-only commit from a code-only
commit from a progress-log commit for one fix).

## Open / not done (additions to the list above)

- Full-corpus precompute run (6 remaining policies) not yet confirmed
  complete as of this log entry — pending.
- `CLAUDE.md` updated to list `check_concern_tags_sync.py` alongside the
  other Build & Run commands.

## Added: "Keeping Documentation in Sync" checklist to CLAUDE.md

Direct user feedback: had to ask, turn by turn, whether README, schema,
infra-baseline, progress files, or pipeline-flow needed updating each
time a change landed, instead of it happening proactively. Considered a
script, but "which docs are relevant to this change" is a judgment
call, not a mechanically checkable state (unlike
`check_concern_tags_sync.py`'s objective drift-check) — landed on an
explicit checklist inside `CLAUDE.md` itself (read at the start of
every session, not just living in personal auto-memory): every doc
file, what it covers, and when to touch it. Also saved as a feedback
memory for future sessions.

## Built: query pipeline step 8 (narrative generation)

`query/narrative_generation.py` + `docs/prompts/prompt_c_narrative.txt`
— Gemini flash-lite explains the top 3 ranked candidates in plain
language, grounded only in each candidate's step 6 narrative chunks,
with pros/cons and a disclosure that shown premiums are estimates, not
quotes. Same `google-genai` client pattern as
`extraction_test/run_layer2_derivation.py`; unlike Voyage, Gemini's free
tier isn't a practical constraint here (one call per query, no
batching).

**Scope note, not an oversight:** covers base plans only, not "plan +
rider combos" as the original design envisioned — steps 3-5 don't
implement rider eligibility/combination yet, so there's nothing yet to
combine with a base plan.

Tested twice against real Layer 1/2 data and a real Gemini call
(deliberately using each top candidate's local `chunks_<policy_id>.json`
file directly for grounding, bypassing Qdrant/Voyage entirely, so as
not to compete with the full-corpus precompute job running in the
background at the same time — the lesson from earlier in this session
about not making parallel Voyage calls during a batched run). Both runs
produced correctly grounded narratives citing real facts (₹50,00,000
cover, 5/10/15-year instalment options, level/increasing SA choice,
30-day free-look period) with no invented claims, never recommended one
candidate over another, and reproduced the disclosure paragraph
verbatim as instructed.

**Bug found and fixed:** the second test run's headings came out as
`### Candidate 1: 875 (Policy ID: 875)` instead of the real plan name.
Root cause: `_format_candidate()`'s own per-candidate context block was
itself headed with a markdown `### Candidate {rank}: ...` heading, and
Gemini sometimes echoed that literal format back rather than following
the prompt's instruction to head its output with just the plan name —
the same failure shape as a prompt's own example value leaking into
extraction output (`docs/progress/20260726-progress.md`'s trap 14
finding: "859" example bleeding into a real extraction). Fixed by
removing the markdown heading syntax from the context block (plain
"Candidate N context (internal reference only - do not use this exact
heading format...)" instead) and tightening the prompt's instruction to
name the exact source field ("Plan name:") to use. Verified fixed on a
third run — all three headings correctly used the real plan name
("LIC's Yuva Term", "LIC's New Tech-Term", "LIC's Digi Term").

## Decided: n8n runtime orchestration - thin layer, not a reimplementation

Discussed and resolved the open question from earlier in this session
(how does the live query pipeline actually run, given n8n is the
already-assumed runtime per `docs/infra-baseline.md`/the "Runtime data
source" note): n8n does NOT reimplement steps 3-8's logic as native
nodes. That logic already represents real, tested effort - including
several bugs only found by actually running it this session (Voyage
batching/retry, rerank tier-assignment, the narrative-generation
heading-echo fix) - and redoing it in n8n's thinner, harder-to-debug
Code-node environment risks silently reintroducing them. Instead, n8n
calls out to a small, self-hosted Python service on the same Oracle VM
that directly imports and runs the already-built `query/` modules.

Single "run query" endpoint vs. one endpoint per step: went with a
single endpoint (in fact two - see below) - steps 3-8 are already a
fixed, sequential chain in the Python code, so decomposing that into
n8n-visible calls would just re-expose an ordering n8n can't
meaningfully reorder anyway.

## Built: service/ - Python query service wrapping steps 3-8, plus a deterministic Q&A frontend for steps 1-2

New top-level `service/` directory:

- `run_query_pipeline.py`: sequences steps 3-8 - precomputed lookup
  first (step 7), live Voyage rerank fallback only for policy_ids
  missing from the precompute table (the rare safety-net path, not the
  routine case), grounding chunks re-fetched separately for step 8 (a
  different purpose than the fallback's chunks, so not reused even when
  they'd overlap - kept simple over premature optimization).
- `main.py`: thin FastAPI wrapper - `/health`, `/query` (steps 3-8
  directly), `/chat` (steps 1-8). Loads Layer 1/2 JSON and the
  precompute table once at process start, matching the existing
  "fresh `git pull` + restart" update model.
- `chat_session.py`: steps 1-2, deterministic Q&A for v1 (fixed
  questions, type/enum parsing, no LLM) - explicitly asked by the user
  to be extensible: "in future versions, I want to build a proper
  frontend that should talk to the user like an llm... at that stage,
  we should be able to extend the solution instead of reworking the
  whole thing." Landed on isolating the field-filling logic behind one
  narrow function, `fill_next_field(state, message)` - the intended
  swap point for a future LLM-based frontend (Ollama/Gemini) - without
  building a formal strategy interface/registry now, which would mean
  guessing at a future implementation's actual shape (e.g. whether it
  needs full conversation history, not just current field state) before
  it exists.
- Session state (steps 1-2) ended up living in the Python service's
  process memory, not n8n's own memory nodes as first discussed - n8n's
  memory nodes are built for LLM conversational history, not raw
  slot-filling state, and splitting this logic across two systems would
  contradict the whole point of the "thin n8n" decision above. Known
  limitation, accepted for this project's stated personal-use scope:
  in-memory + single-process means state is lost on a restart and
  doesn't scale past one worker.

**Bug found and fixed:** an early version of `_parse_field()` required
numeric replies (age/sum_assured/term/budget) to be the *entire* reply
string (`int()`/`float()` on the stripped text) - failed on completely
reasonable natural phrasing like "20 years" or "10000 per year", only
succeeding on a bare digit string. Fixed by extracting the first number
from surrounding text via regex (matching how `concern_tags` parsing
already worked), verified against a full re-run of the same test
sequence: "20 years" -> 20, "regular please" -> "regular", "50,00,000"
(Indian comma-grouping) -> 500000, all correct.

**Tested, all locally, no VM/n8n involved yet:**
- `run_query_pipeline.run_query()` end-to-end, using synthetic full
  precompute coverage (since the real 6-policy precompute job was still
  running) to exercise the "no missing policies" path - the routine
  case once precompute is complete. Correctly grounded, correctly
  ordered result.
- The FastAPI server itself: starts cleanly, `/health` reports all 7
  policies loaded.
- `/chat`'s full slot-filling sequence through all 6 questions,
  including a deliberate bad reply to confirm re-asking instead of
  crashing. Stopped short of the final completing message each time (to
  avoid triggering `run_query()`'s live Voyage calls while the
  background precompute job was still active - the same
  don't-compete-for-the-rate-limit lesson from earlier this session).

**Not yet done:** the actual n8n workflow (Chat Trigger -> HTTP Request
-> Respond) hasn't been built in n8n; `service/` hasn't been deployed
to the Oracle VM; a `/chat` conversation hasn't been run all the way to
a real `run_query()` completion against genuine (non-synthetic)
precomputed data; BYOK (step 10) remains entirely unbuilt.

## Incident: full-corpus precompute run lost twice, root-caused and fixed both times

**Loss #1 - laptop sleep, ~1.5 hours of work, entirely lost.** The
6-remaining-policy precompute run (started earlier, see above) was
killed when the laptop went to sleep mid-run (confirmed via the system
clock jumping ~2.5 hours across the interruption, not just a brief
glitch) - `caffeinate -i` was running but only blocks *automatic idle*
sleep, not a manual sleep or lid-close, which always sleeps a MacBook
regardless. Checked the actual state after: `precomputed_rerank_scores.json`
still only had the original 1-policy (875) test data - the run had
reached 7/8 tags before dying, but **none of that was ever saved**,
because the original `precompute_rerank_scores.py` only wrote to disk
once, after all 8 tags finished. Fixed (see `precompute_and_save()`):
checkpoints to disk after every tag, and skips any tag whose chunks are
already fully covered on restart - a future interruption now costs at
most one tag's work, not the whole run.

**Decision: move this class of job to the Oracle VM.** Prompted by the
user directly asking why this wasn't already running there, given it's
meant to run 24/7 and wouldn't care about laptop sleep. Found the VM
did NOT actually have a clone of this repo yet, despite
`docs/infra-baseline.md` describing that as the intended design (an
item flagged as unverified as far back as the 2026-07-27 log). Set up
for the first time: cloned via plain `git clone` (no PAT needed - this
repo is public, confirmed via the GitHub API, and the VM only ever
reads/pulls, never pushes - simpler than the credential-helper plan
`infra-baseline.md` had assumed would be necessary), installed Python
deps (`pip` itself wasn't even present - `apt-get install python3-pip`
first), transferred `.env` via `scp` (never typed into a command,
`chmod 600` after), confirmed `QDRANT_URL=http://localhost:6333` works
unchanged since Qdrant runs locally on the VM too (no tunnel needed
there). Committed and pushed the checkpointing fix first, so the VM's
clone would have it before running.

**Loss #2 - VM launch method itself was wrong, ~25 min of work, entirely
lost again, same session.** Launched via `nohup command > log 2>&1 &
disown` over SSH, confirmed running post-disconnect, went to sleep for
the night. Woken by the user's distrust that tag 1 had run correctly -
checked, and found the process had actually died silently ~25 minutes
in, mid-way through tag 2 (`debt_linked_cover`), with **zero error
output** in the log. Ruled out: VM reboot (`uptime` showed 24 days, no
reboot), OOM kill (nothing in `dmesg`/`journalctl`). Root cause: `nohup`
+ `disown` alone don't protect against `systemd-logind` tearing down
the SSH session's cgroup scope when the connection ends - confirmed
`loginctl show-user ubuntu` reported `Linger=no`. Fixed: `sudo loginctl
enable-linger ubuntu` (lets the user's systemd instance persist past
logout) plus launching via `setsid nohup command > log 2>&1 < /dev/null
& disown` (fully detaches into its own session, not just SIGHUP-proof).

**User's response to the distrust, honored directly rather than
debated:** wipe `precomputed_rerank_scores.json` entirely (including
875's original, already-verified data - tag 1 `income_replacement` is
shared across all policies under one key, so if that tag was in doubt
it applied there too) and restart the full 7-policy precompute from
absolute zero on the VM, launched with the `setsid`/lingering fix.
Verified alive well past the ~25-minute mark where the previous attempt
died silently, still running as this log entry is written.

**Also discussed, not yet acted on:** the user ran `sudo apt upgrade` on
the VM directly mid-run (their own terminal session, a different SSH
key than the one used for this session's automation) - confirmed the
precompute job survived it untouched. Flagged a pending kernel upgrade
(6.17.0-1018 -> 1019) and several deferred service restarts, including
`systemd-logind.service` - recommended NOT rebooting until the
precompute job finishes (a reboot would kill it, unlike the
session-scope issue above), since the kernel bump isn't flagged as an
urgent security fix. Confirmed for the user: no unexpected cost risk
from leaving this running overnight - Oracle's Always Free tier is
billed by allocation, not uptime, Voyage's calls are on its free tier
and nowhere near the 200M-token/month allowance, and the job's actual
CPU/network footprint is negligible (mostly asleep, pacing for Voyage's
rate limit). The one real (non-cost) risk named: `unattended-upgrades`
autonomously restarting `systemd-logind` or rebooting overnight would
still kill the job the same way loss #2 did - accepted as a possibility
rather than solved, would just require noticing and restarting again.

## Open / not done (additions)

- Full 7-policy precompute run in progress on the Oracle VM as of this
  log entry (fresh restart from zero, launched via
  `setsid`/`loginctl enable-linger` this time) - not yet confirmed
  complete.
- `/chat` still hasn't been run to a real completion against genuine
  precomputed data - blocked on the above finishing.
- `service/` (the FastAPI app itself) is still local-only - only the
  precompute *batch job* has been moved to the VM tonight, not the
  query-serving service.
