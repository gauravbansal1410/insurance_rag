# 2026-07-07 Progress Log

## Step 2 verification (complete)
- Voyage AI: `voyage-law-2` and `rerank-2.5-lite` confirmed current, not deprecated.
- Gemini: 3.1 Pro lost free-tier API access April 1, 2026 ($2/$12 per 1M tokens,
  $4/$18 above 200K context). No impact on recurring cost target - Pro is scoped
  to one-time extraction only, already budgeted as a paid step.
- Oracle VM: confirmed Ampere shape, 11Gi total RAM, 10Gi available at idle.
  Headroom is not a constraint for Qdrant at this corpus's scale.

## Unplanned infra maintenance
- Applied 16 pending OS updates (`apt upgrade`), rebooted VM.
- Discovered n8n container had no restart policy - did not survive reboot,
  required manual `docker start n8n`.
- Fixed with `docker update --restart unless-stopped n8n` (confirmed via
  `docker inspect`). Container will now survive future reboots automatically.
- nginx confirmed auto-starts via systemd, unaffected.
- Verified all 3 workflows intact post-reboot: `interview_bot_debrief`,
  `interview_bot`, `linkedIn-job-scrapper_v4` (published, unchanged).
- Manually triggered `interview_bot` end-to-end post-fix - confirmed working.
- `linkedIn-job-scrapper_v4` not yet live-tested post-reboot (runs on 8am
  schedule) - confirm on next scheduled run.

## Docs updated
- infra-baseline.md: added `--restart unless-stopped` to docker run reference,
  replaced "verify RAM" open item with confirmed 11Gi/Ampere spec.
- architecture.md: marked Step 2 verification items resolved.

## Next
- Confirm tomorrow's 8am scraper run completes without error.
- Begin PDF ingestion pipeline design (architecture/schema discussion, this thread).
