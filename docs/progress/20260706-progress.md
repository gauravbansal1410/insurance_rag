# 20260706-progress.md

## Session: Repo setup (Step 1)

### Completed
- Created `insurance_rag` GitHub repo (public, no PII per architecture doc).
- Generated fine-grained PAT scoped to `insurance_rag` only, Contents: Read/write, 90-day expiration.
- Cloned repo locally to `~/Desktop/claude/personal/insurance_rag` via Claude Code.
- Set local (non-global) git identity: Gaurav Bansal / gauravbansal1410@gmail.com.
- Created `docs/` with `architecture.md` and `infra-baseline.md` (real content, after catching Claude Code's initial generic placeholders).
- Fixed doc/reality mismatch: architecture.md said `/raw-pdfs/`, actual folder is `raw_pdfs` - doc updated to match.
- Added `.gitignore` before first commit, catching `.DS_Store` files and `.claude/` that would've otherwise been committed.
- Committed and pushed `CLAUDE.md`, `docs/`, `raw_pdfs/` (LIC PDF corpus - term/money-back/whole-life/endowment/rider categories).
- `CLAUDE.md` includes a standing rule: never suggest embedding credentials in git remote URLs, use osxkeychain or interactive auth instead.
- Confirmed no embedded token in `.git/config` - credential handled via macOS Keychain.
- Removed `raw_pdfs/raw_summaries/*.csv` - manual, unreliable PDF-to-CSV summaries from before this project's formal pipeline existed. Deleted locally and via git (`0a14b8e`), confirmed gone on GitHub.

### Deferred, not forgotten
- PDF filenames are manual/inconsistent. Rename logic will piggyback on the Gemini extraction step later rather than get built separately now.
- Never tested whether the CLAUDE.md credential rule changes Claude Code's actual behavior on a real auth failure (Keychain had already cached the credential). Low stakes, deliberately skipped.

### Lessons worth remembering
- Claude Code's "ready"/"clean"/"done" claims were wrong or unverified multiple times this session (placeholder docs, staged `.DS_Store`, missing `.claude/` ignore, "clean slate" needing independent GitHub confirmation). Verify with actual command output every time.
- Claude Code and claude.ai Project have zero sync - docs must be updated by hand in both places.

### Next session (Step 2 - new chat)
Verify before building:
- Current Voyage model names for `voyage-law-2` and `rerank-2.5-lite`, free-tier ceilings.
- Current Gemini 3.1 Pro pricing and rate limits.
- Actual free RAM on the Oracle VM shape in use.
